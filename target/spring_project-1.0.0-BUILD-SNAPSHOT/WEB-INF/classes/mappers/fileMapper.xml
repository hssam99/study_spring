<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koreait.www.repository.FileDAO">
    <insert id="insert" parameterType="FileVO">
        INSERT INTO file (uuid, save_dir, file_name, file_type, bno, file_size)
        VALUES (#{uuid}, #{saveDir}, #{fileName}, #{fileType}, #{bno}, #{fileSize})
    </insert>
    <select id="getList" parameterType="long" resultType="FileVO">
        SELECT * FROM file WHERE bno = #{bno}
    </select>

    <delete id="deleteFile">
        DELETE FROM file WHERE uuid = #{uuid}
    </delete>
    <select id="getFile" resultType="FileVO" parameterType="string">
        SELECT * FROM file WHERE uuid = #{uuid}
    </select>
    <select id="getFilesByDate" resultType="FileVO" parameterType="string">
        SELECT * FROM file WHERE save_dir = #{today}
    </select>

</mapper>